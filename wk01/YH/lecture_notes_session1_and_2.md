<개념정리 - Do it! 알고리즘 코딩테스트 with Python 1강 ~ 10강(섹션 1~2)>



<섹션 1> 코딩테스트 준비하기

1. 시간 복잡도

- 어떤 알고리즘으로 풀어야 할까? → 알고리즘 선택의 기준이 되는 "시간 복잡도"

-시간 복잡도: 주어진 문제를 해결하기 위한 연산 횟수. 파이썬에서는 2000만~1억번의 연산을 1초의 수행 시간으로 예측 가능
* 연산 횟수 = 알고리즘 시간 복잡도 N값에 데이터의 최대 크기를 대입하여 도출

- 시간 복잡도 유형:
  - 빅-오메가(Ω(n)) : 최선일 때(best case)의 연산 횟수. 시간 복잡도 = 1
  - 빅-세타(θ(n)) : 보통일 때(average case)의 연산 횟수. 시간 복잡도 = N/2
  - 빅-오(O(n)) : 보통일 때(worst case)의 연산 횟수. 시간 복잡도 = N
  (ex) findNumber = random.randrange(1, 101) → 데이터 개수 = 100개 ☞ Ω(n) = 1, θ(n) = 100/2 = 50, O(n) = 100
※ 코딩 테스트에서는 빅-오(O(n)) 표기법을 기준으로 수행 시간을 계산하는 것이 좋음!(다양한 테스트 케이스를 수행해 모든 케이스를 통과해야 합격 → "최악"의 경우를 염두해야 함)

(ex) 파이썬 사용, 시간 제한 2초, 데이터 크기 = 1,000,000 인 문제 해결 시
 → 최대 2,000만 * 2 = 4,000만 이하의 연산 횟수로 문제 해결
 → 버블 정렬을 사용할 경우: O(n^2) = (10^6)^2 = 10^12 > 40,000,000 → 부적합!
     병합 정렬을 사용할 경우: O(nlogn) = 약 2,000만 < 40,000,000 → 적합!

- 시간 복잡도 도출 기준:
 (1) 상수는 시간 복잡도 계산에서 제외
 (2) 가장 많이 중첩된 반복문의 수행 횟수가 시간 복잡도의 기준이 됨



2. 디버깅

- 코드의 논리 오류를 어떻게 잡을까? → 가장 뛰어난 오류 탐색 방법, "디버깅"
  - 문법 오류: 컴파일러가 잡아줌 / 논리 오류(문법은 맞으나 logic이 원하는 방향으로 돌지 않음): 단계별로 검증 과정이 필요

- 디버깅: 프로그램에서 발생하는 문법 오류나 논리 오류를 찾아 바로잡는 과정

- 방법: 중단점(break point)을 설정하고 디버깅(여러 개 설정 가능). 이 괒어에서 변숫값 지정 가능(Variables 기능)

- 변수 초기화 오류: 변수 초기화 로직에서 초기화를 제대로 하지 않은 경우
- 반복문에서 인덱스 범위 지정 오류: 반복 범위를 잘못 지정하거나 비교 연산자(<. <=)를 잘못 사용하는 경우
-  잘못된 변수 사용 오류: 출력 부분이나 로직 안에서 사용해야 하는 변수를 다른 변수와 혼동하여 잘못 사용하는 경우
- 파이썬 자동 형 변환 조성: 데이터 계산 도중 다양한 연산이 수행될 때 예상하지 못한 형 변환 발생 가능

* 파이썬에서의 나누기 연산자는 두가지임(/, //)
- / 연산: 나눗셈을 한 결괏값을 float형으로 출력하여 소숫점 결과가지 보여줌
- // 연산: 나눗셈을 한결괏값으 int형으로 출력하여 몫의 결과만 보여줌
- % 연산: 나눗셈을 한 후 나눈 나머지 값을 보여줌




<섹션 2> 자료 구조(Data structure)

1. 배열과 리스트

- 배열: 메모리의 연속 공간에 값이 채워져 있는 형태의 자료 구조. 
  - 배열의 값은 인덱스를 통해 참조할 수 있으며, 선언한 자료형의 값만 저장 할 수 있음.
  - 새로운 값을 삽입하거나 특정 인덱스에 있는 값을 삭제하기 어려움.
  - 배열의 크기는 선언할 떄 지정할 수 있으며, 한 번 선언하면 크기를 늘리거나 줄일 수 없음.

- 리스트: 값과 포인터를 묶은 노드를 포인터로 연결한 자료구조.
  - 인덱스가 없으므로 값에 접근하려면 Head 포인터부터 순서대로 접근해야함. → 접근 속도가 느림
  - 포인터로 연결되어 있어 데이터를 삽입하거나 삭제하는 연산 속도가 빠름.
  - 선언할 때 크기를 별도로 지정하지 않아도 됨. 즉, 리스트의 크기는 정해져 있지 않으며, 크기가 변하기 쉬운 데이터를 다룰 때 적절.
  - 포인터를 저장할 공간이 필요하므로 배열보다 구조가 복잡.

* 파이썬에서는 리스트가 배열의 특성도 함께 내포하고 있어 크게 구분하여 사용하지는 않음. 파이썬의 리스트는 리스트와 배열의 장점을 모두 갖고 있음.(index로 바로 접근 가능, 가변적인 성격을 가짐)

* 백준 11720번 - 숫자의 합 구하기
- 문제 분석: 주어진 숫자를 리스트의 형태로 저장한 뒤 해당 리스트를 index를 이용해 탐색하면서 각 자릿수의 값을 더하면 됨. 자릿수를 더할 때는 정수형으로 변환해 더함.
 (1) 숫자의 개수 만큼 입력받은 값을 리스트 형태로 저장. → list(input())
 (2) numbers 리스트를 탐색하며 각 값을 정수형으로 변환하며 결괏값에 더하여 누적.

- sudo code: { 
n 값 받기
numbers 변수에 list 함수를 이용하여 숫자를 한 자리씩 나누어 받기
sum 변수 선언

for numbers 탐색:
   sum 변수에 numbers에 있는 각 자릿수를 가져와 더하기

sum 출력 
}

* 형 변환: int, float, str, chr, bool
  ex) int 형 변환: int(data) → 다른 형도 동일 형식 (형(data))

* 형 변환 가능 여부
- int: float, bool 변환 가능
- float: int bool 변환 가능
- str: int float, bool, chr 변환 가능
- chr: int, bool 변환 가능
- bool: int, float, str, chr 변환 가능



2. 구간 합

- 구간 합: 합 배열을 이용하여 시간 복잡도를 더 줄이기 위해 사용하는 특수한 목적의 알고리즘 → 합 배열을 구해야 함.

- 합 배열: S[i] = A[0] + A[1] + A[2] + ... + A[i-1] + A[i] → 기존의 배열을 전처리한 배열
- 합 배열을 미리 구해 놓으면 기존 배열의 일정 범위의 합을 구하는 시간 복잡도가 O(n)에서 O(1)로 감소.
- 합 배열 S를 만드는 공식: S[i] = S[i-1] + A[i]

- 구간 합을 구하는 공식: S[j] - S[i-1] → i에서 j까지 구간 합

* 백준 11660번 - 구간 합 구하기 2
- 문제 분석: 질의의 개수가 100,000개 이므로 문제의 질의마다 합을 구하면 안 되고, 구간 합 배열을 이용해야 함. 구간 합 배열이 1차원에서 2차원으로 학장된 것으로 생각하여 구간 합 배열을 어떻게 구성할지 고민해야 함!   
- 2차원 구간 합 배열 D[X][Y] = 원본 배열의 (0,0)부터 (X,Y)까지의 사각형 영역 안에 있는 수의 합
- D[i][j]의 값을 채우는 구간 합 공식: D[i][j] = D[i][j-1] + D[i-1][j] - D[i-1][j-1] + A[i][j]
- X_1, Y_1, X_2, Y_2를 구간 합으로 구하는 방법: D[X_2][Y_2] - D[X_1 - 1][Y_2] - D[X_2][Y_1 - 1] - D[X_1 - 1][Y_1 - 1]
 
- sudo code: { 
n(리스트 크기) m(질의 수)
A(원본 리스트), D(합 배열)

for n 만큼 반복:
    원본 리스트 데이터 저장

for i를 1부터 n까지 반복:
   for j를 1부터 n까지 반복:
      합 배열 저장
      D[i][j] = D[i][j-1] + D[i-1][j] - D[i-1][j-1] + A[i][j];

for m만큼 반복:
   질의에 대한 결과 계산 및 출력
   결과 = D[X_2][Y_2] - D[X_1 - 1][Y_2] - D[X_2][Y_1 - 1] - D[X_1 - 1][Y_1 - 1]
}

* 백준 10986번 - 나머지 합 구하기
- 문제 분석: N의 최댓값이 106이라 연산량이 작게 느껴질 수 있으나, 106개의 수에 대하여 모든 구간 합을 구해야하므로 1초 안에 연산하기 어려움. → 구간 합 배열 이용!

- 나머지 합 문제 풀이 아이디어
  - (A+B) % C = {(A%C) + (B%C)} % C → 특정 구간 수들의 나머지 연산을 더해 나머지 연산을 한 값과 이 구간의 합의 나머지 연산을 한 값은 동일함!
  - 구간 합 배열을 이용한 식 S[i] - S[j]는 원본 리스트의 j+1부터 i까지의 구간 합
  - S[i] % M의 값과 S[j] % M의 값이 같다면 (S[i] - S[j]) % M = 0
 - 구간 합 배열의 원소를 M으로 나눈 나머지로 업데이트하고 S[i]와 S[j]가 같은 (i, j) 쌍을 찾으면 원본 리스트에서 j+1부터 i까지의 구간 합이 M으로 나누어 떨어짐.

- sudo code: { 
n 입력받기(수열의 개수)
m 입력 받기(나누어떨어져야 하는 수)
A 입력 받기(원본 수열 저장 리스트)
S 선언하기(합 배열)
C 선언하기(같은 나머지의 인덱스를 카운트하는 리스트)
answer 선언하기(정답 변수)

for i → 1 ~ n-1;
   S[i] = S[i-1] + A[i] → 합 배열 저장

for i → 0 ~ n-1;
   remainder = S[i] %n → 합 배열을 M으로 나눈 나머지 값
   if(remainder == 0) 정답을 1 증가
   C(remainder)의 값을 1 증가

for i → 0 ~ n-1;
   C[i](i가 나머지인 인덱스의 개수)에서 2가지를 뽑는 경우의 수를 정답에 더하기
     → C[i]개 중 2개를 뽑는 경우의 수 계산 공식: C[i] * (C[i-1]) / 2

결괏값(answer) 출력
}

* 백준 11003번 - 최솟값 찾기 1
- 문제 분석: 일정 범위 안에서 최솟값을 구하는 문제 이므로 슬라이딩 윈도우와 정렬을 사용하면 됨. 윈도우의 크기는 무제에서 최솟값을 구하는 범위가 i-L+1부터 i까지이므로 L로 생각. 문제에서 N과 L의 최대 범위가 5,000,000이므로 이 문제에서는 정렬 사용 불가(정렬은 일반적으로 O(nlogn)의 시간 복잡도를 가지므로) → O(n)의 시간 복잡도로 해결해야 함. but 슬라이딩 윈도우는 덱(deque)으로 구현하여 정렬 효과를 볼 수 있음.(덱: 약 끝에서 데이터를 삽입하거나 삭제할 수 있는 자료구조) → 왼쪽에서는 appendleft(), popleft() 함수가 삽입, 삭제 역할을 하고, 오른쪽에서는 append(), pop() 함수가 삽입, 삭제 역할을 함.

- 출력 과정
  (1) 최초 (1,1)이 덱에 추가 되면 비교 대상이 없고, 범위도 만족하므로 바로 1을 출력.
  (2) (2,5)는 (1,1)과 숫자를 비교했을 때 더 크므로 탐색을 멈추고 덱에 추가. 인덱스 범위가 1~3ㅣ여서 윈도우 범위를 만족하므로 다시 1을 출력
  (3) (3,2)는 (2,5)와 숫자를 비교했을 때 더 작으므로 (2,5)를 덱에서 제거. (1,1)은 여전히 (3,2)보다 숫자가 작으므로 탐색을 멈추고 (3,2)를 덱에 저장. 덱의 상태는 (1,1), (3,2)가 되고, 인덱스 범위 1~3 역시 윈도우 범위를 만족하므로 다시 1을 출력.
  (4) (이러한 과정 이어서 ~)

- sudo code: { 
N(데이터 개수) L(최솟값을 구하는 범위)
mydeque(데이터를 담을 덱 자료구조)
now(주어진 숫자 데이터를 가지는 리스트)

for N만큼 반복: → now 리스트를 탐색(now[i]를 현재 값으로 세팅)
   덱의 마지막 위치에서부터 현재 값보다 큰 값은 덱에서 제거
   덱의 마짐가 위치에 현재 값 저장
   덱의 1번째 위치에서부터 L의 범위를 벗어난 값(now index-L <= index)을 덱에서 제거
   덱의 1번째 데이터 출력
}



3. 스택과 큐

- 스택과 큐: 리스트에서 조금 더 발전한 형태의 자료구조. 구조는 비슷하지만 처리 방식이 서로 다름.

- 스택: 삽입과 삭제 연산이 후입선출(LIFO)로 이루어지는 자료구조. 삽입과 삭제가 한 쪽에서만 일어남.

- 파이썬에서의 스택
  - 위치: top(삽입과 삭제가 일어나는 위치)
  - 연산(리스트 이름이 s일 때):
    (1) s.append(data): top위치에 새로운 데이터를 삽입
    (2) s.pop(): top위치에 현재 있는 데이터를 삭제하고 확인
    (3) s[-1]: top위치에 현재 있는 데이터를 단순 확인

- 스택은 깊이 우선 탐색(DFS, Depth First Search), 백트래킹 종류의 코딩 테스트에 효과적.

- 큐: 삽입과 삭제 연산이 선입선출(FIFO)로 이루어지는 자료구조. 스택과 다르게 먼저 들어온 데이터가 먼저 나감 → 삽입과 삭제가 양방향에서 이루어짐.
- 파이썬에서는 일반적으로 deque를 이용하여 큐를 구현.

- 파이썬에서의 큐
  - 위치: rear(큐에서 가장 끝 데이터를 가리키는 영역), front(큐에서 가장 앞의 데이터를 가리키는 영역)
  - 연산(리스트 이름이 s일 때):
    (1) s.append(data): rear 부분에 새로운 데이터를 삽입
    (2) s.popleft(): front 부분에 있는 데이터를 삭제하고 확인
    (3) s[0]: 큐의 맨 앞(front)에 있는 데이터를 확인

- 큐는 너비 우선 탐색(BFS, Breadth First Search)에서 자주 사용.

- 우선순위 큐(priority queue): 값이 들어간 순서와 상관 없이 우선순위가 높은 데이터가 먼저 나오는 자료구조. 큐 설정에 따라 front에 항상 최댓값 또는 최솟값이 위치. 일반적으로 힙(Max heap, Min heap)을 이용해 구현.

* 백준 17298번 - 오큰수 구하기
- 문제 분석: N의 크기가 1,000,000이므로 반복문으로 오큰수를 찾으면 제한 시간을 초과함. 따라서 "(1) 스택에 새로 들어오는 수가 top에 존재하는 수보다 크면 그 수는 오큰수가 되고, (2) 오큰수를 구한 후 수열에서 오큰수가 존재하지 않는 숫자에 -1을 출력해야 한다"는 아이디어를 추가하여 구현.

- 출력 과정
  (1) 최초 (1,1)이 덱에 추가 되면 비교 대상이 없고, 범위도 만족하므로 바로 1을 출력.
  (2) (2,5)는 (1,1)과 숫자를 비교했을 때 더 크므로 탐색을 멈추고 덱에 추가. 인덱스 범위가 1~3ㅣ여서 윈도우 범위를 만족하므로 다시 1을 출력
  (3) (3,2)는 (2,5)와 숫자를 비교했을 때 더 작으므로 (2,5)를 덱에서 제거. (1,1)은 여전히 (3,2)보다 숫자가 작으므로 탐색을 멈추고 (3,2)를 덱에 저장. 덱의 상태는 (1,1), (3,2)가 되고, 인덱스 범위 1~3 역시 윈도우 범위를 만족하므로 다시 1을 출력.
  (4) (이러한 과정 이어서 ~)

- sudo code: { 
N(수열 개수) A(수열 리스트) ans(정답 리스트)
A 수열 리스트 채우기
myStack(스택 선언)

for i를 N만큼 반복:
   while 스택이 비지 않고, 현재 수열값이 top에 해당하는 수열보다 클 때까지:
      스택에서 pop한 값을 index로 하는 정답리스트 부분을 수열 리스트의 현재값(A[i])으로 저장
   스택에 i의 값을 저장

while 스택이 빌 떄까지:
   스택에 있는 index의 정답 리스트에 -1 저장

정답 리스트 출력

}
